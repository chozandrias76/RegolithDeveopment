using UnityEngine;
using System.Collections;
using System;

public class CraftingHandler : MonoBehaviour {
	/*	string[] carpentryMaterials =
		{
			"Wood",
			"Plank",
			"Handle",
			"Rod",
			"Wood Axe Head",
			"Wood Hoe Head",
			"Wood Shovel Head",
			"Wood Pickaxe Head",
			"Stone Axe Head",
			"Stone Hoe Head",
			"Stone Shovel Head",
			"Stone Pickaxe Head"
		};
	public enum craftableItems
	{
			Plank,
			Handle,
			Rod,
			Chisel,
			WoodAxeHead,
			WoodHoeHead,
			WoodShovelHead,
			WoodPickaxeHead,
			StoneAxeHead,
			StoneHoeHead,
			StoneShovelHead,
			StonePickaxeHead,
			WoodAxe,
			WoodHoe,
			WoodShovel,
			WoodPickaxe,
			StoneAxe,
			StoneHoe,
			StoneShovel,
			StonePickaxe
		
	}
public static CraftingHandler Carpentry(bool rock, bool chisel, bool saw, carpentryMaterials cMat){
		if(cMats[0])if(saw){
			Messenger.Broadcast("Craft " + craftableItems.Plank);
		}
		if(cMats[1])if(chisel||rock){
			//switch(
			Messenger.Broadcast("Craft " + craftableItems.Handle);
			Messenger.Broadcast("Craft " + craftableItems.Rod);
		}
		if(cMats[2])if(cMats[3])if(chisel||rock){
			Messenger.Broadcast("Craft " + craftableItems.Chisel);
		}
		if(cMats[0])if(saw){
			Messenger.Broadcast("Craft " + craftableItems);
		}
		if(cMats[0])if(saw){
			Messenger.Broadcast("Craft " + craftableItems);
		}
		if(cMats[0])if(saw){
			Messenger.Broadcast("Craft " + craftableItems);
		}
	}*/
}
